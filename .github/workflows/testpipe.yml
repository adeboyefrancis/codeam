name: CI/CD Test Job from Codeam Labs

on: [push]

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: |
        echo "Hello world from CodeDamn" \
        tree
  